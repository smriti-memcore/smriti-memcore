# Smithery configuration file for smriti-memcore
# See https://smithery.ai/docs for schema details

startCommand:
  type: stdio
  commandFunction: |-
    (config) => ({
      command: 'uvx',
      args: ['smriti-memcore']
    })
